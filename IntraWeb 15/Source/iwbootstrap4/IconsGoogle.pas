unit IconsGoogle;

interface

const
  GOOGLE_ICONS1 =
    '3d_rotation=0' + sLineBreak + // &#xe84d;
    'ac_unit=0' + sLineBreak + // &#xeb3b;
    'access_alarm=0' + sLineBreak + // &#xe190;
    'access_alarms=0' + sLineBreak + // &#xe191;
    'access_time=0' + sLineBreak + // &#xe192;
    'accessibility=0' + sLineBreak + // &#xe84e;
    'accessible=0' + sLineBreak + // &#;
    'account_balance=0' + sLineBreak + // &#xe84f;
    'account_balance_wallet=0' + sLineBreak + // &#xe850;
    'account_box=0' + sLineBreak + // &#xe851;
    'account_circle=0' + sLineBreak + // &#xe853;
    'adb=0' + sLineBreak + // &#xe60e;
    'add=0' + sLineBreak + // &#xe145;
    'add_a_photo=0' + sLineBreak + // &#xe439;
    'add_alarm=0' + sLineBreak + // &#xe193;
    'add_alert=0' + sLineBreak + // &#xe003;
    'add_box=0' + sLineBreak + // &#xe146;
    'add_circle=0' + sLineBreak + // &#xe147;
    'add_circle_outline=0' + sLineBreak + // &#xe148;
    'add_shopping_cart=0' + sLineBreak + // &#xe854;
    'add_to_photos=0' + sLineBreak + // &#xe39d;
    'add_to_queue=0' + sLineBreak + // &#xe05c;
    'adjust=0' + sLineBreak + // &#xe39e;
    'airline_seat_flat=0' + sLineBreak + // &#xe630;
    'airline_seat_flat_angled=0' + sLineBreak + // &#xe631;
    'airline_seat_individual_suite=0' + sLineBreak + // &#xe632;
    'airline_seat_legroom_extra=0' + sLineBreak + // &#xe633;
    'airline_seat_legroom_normal=0' + sLineBreak + // &#xe634;
    'airline_seat_legroom_reduced=0' + sLineBreak + // &#xe635;
    'airline_seat_recline_extra=0' + sLineBreak + // &#xe636;
    'airline_seat_recline_normal=0' + sLineBreak + // &#xe637;
    'airplanemode_active=0' + sLineBreak + // &#xe195;
    'airplanemode_inactive=0' + sLineBreak + // &#xe194;
    'airplay=0' + sLineBreak + // &#xe055;
    'airport_shuttle=0' + sLineBreak + // &#xeb3c;
    'alarm=0' + sLineBreak + // &#xe855;
    'alarm_add=0' + sLineBreak + // &#xe856;
    'alarm_off=0' + sLineBreak + // &#xe857;
    'alarm_on=0' + sLineBreak + // &#xe858;
    'album=0' + sLineBreak + // &#xe019;
    'all_out=0' + sLineBreak + // &#xe90b;
    'all_inclusive=0' + sLineBreak + // &#xebsd;
    'android=0' + sLineBreak + // &#xe859;
    'announcement=0' + sLineBreak + // &#xe85a;
    'apps=0' + sLineBreak + // &#xe5c3;
    'archive=0' + sLineBreak + // &#xe149;
    'arrow_back=0' + sLineBreak + // &#xe5c4;
    'arrow_drop_down=0' + sLineBreak + // &#xe5c5;
    'arrow_drop_down_circle=0' + sLineBreak + // &#xe5c6;
    'arrow_drop_up=0' + sLineBreak + // &#xe5c7;
    'arrow_downward=0' + sLineBreak + // &#xe5db;
    'arrow_forward=0' + sLineBreak + // &#xe5c8;
    'arrow_upward=0' + sLineBreak + // &#xe5d8;
    'art_track=0' + sLineBreak + // &#xe060;
    'aspect_ratio=0' + sLineBreak + // &#xe85b;
    'assessment=0' + sLineBreak + // &#xe85c;
    'assignment=0' + sLineBreak + // &#xe85d;
    'assignment_ind=0' + sLineBreak + // &#xe85e;
    'assignment_late=0' + sLineBreak + // &#xe85f;
    'assignment_return=0' + sLineBreak + // &#xe860;
    'assignment_returned=0' + sLineBreak + // &#xe861;
    'assignment_turned_in=0' + sLineBreak + // &#xe862;
    'assistant=0' + sLineBreak + // &#xe39f;
    'assistant_photo=0' + sLineBreak + // &#xe3a0;
    'attach_file=0' + sLineBreak + // &#xe226;
    'attach_money=0' + sLineBreak + // &#xe227;
    'attachment=0' + sLineBreak + // &#xe25c;
    'audiotrack=0' + sLineBreak + // &#xe3a1;
    'autorenew=0' + sLineBreak + // &#xe863;
    'av_timer=0' + sLineBreak + // &#xe01b;
    'backspace=0' + sLineBreak + // &#xe14a;
    'backup=0' + sLineBreak + // &#xe864;
    'battery_alert=0' + sLineBreak + // &#xe19c;
    'battery_charging_full=0' + sLineBreak + // &#xe1a3;
    'battery_full=0' + sLineBreak + // &#xe1a4;
    'battery_std=0' + sLineBreak + // &#xe1a5;
    'battery_unknown=0' + sLineBreak + // &#xe1a6;
    'beach_access=0' + sLineBreak + // &#xeb3e;
    'beenhere=0' + sLineBreak + // &#xe52d;
    'block=0' + sLineBreak + // &#xe14b;
    'bluetooth=0' + sLineBreak + // &#xe1a7;
    'bluetooth_audio=0' + sLineBreak + // &#xe60f;
    'bluetooth_connected=0' + sLineBreak + // &#xe1a8;
    'bluetooth_disabled=0' + sLineBreak + // &#xe1a9;
    'bluetooth_searching=0' + sLineBreak + // &#xe1aa;
    'blur_circular=0' + sLineBreak + // &#xe3a2;
    'blur_linear=0' + sLineBreak + // &#xe3a3;
    'blur_off=0' + sLineBreak + // &#xe3a4;
    'blur_on=0' + sLineBreak + // &#xe3a5;
    'book=0' + sLineBreak + // &#xe865;
    'bookmark=0' + sLineBreak + // &#xe866;
    'bookmark_border=0' + sLineBreak + // &#xe867;
    'border_all=0' + sLineBreak + // &#xe228;
    'border_bottom=0' + sLineBreak; // &#xe229;
  GOOGLE_ICONS2 =
    'border_clear=0' + sLineBreak + // &#xe22a;
    'border_color=0' + sLineBreak + // &#xe22b;
    'border_horizontal=0' + sLineBreak + // &#xe22c;
    'border_inner=0' + sLineBreak + // &#xe22d;
    'border_left=0' + sLineBreak + // &#xe22e;
    'border_outer=0' + sLineBreak + // &#xe22f;
    'border_right=0' + sLineBreak + // &#xe230;
    'border_style=0' + sLineBreak + // &#xe231;
    'border_top=0' + sLineBreak + // &#xe232;
    'border_vertical=0' + sLineBreak + // &#xe233;
    'branding_watermark=0' + sLineBreak + // &#xe06b;
    'brightness_auto=0' + sLineBreak + // &#xe1ab;
    'brightness_1=0' + sLineBreak + // &#xe3a6;
    'brightness_2=0' + sLineBreak + // &#xe3a7;
    'brightness_3=0' + sLineBreak + // &#xe3a8;
    'brightness_4=0' + sLineBreak + // &#xe3a9;
    'brightness_5=0' + sLineBreak + // &#xe3aa;
    'brightness_6=0' + sLineBreak + // &#xe3ab;
    'brightness_7=0' + sLineBreak + // &#xe3ac;
    'brightness_high=0' + sLineBreak + // &#xe1ac;
    'brightness_low=0' + sLineBreak + // &#xe1ad;
    'brightness_medium=0' + sLineBreak + // &#xe1ae;
    'broken_image=0' + sLineBreak + // &#xe3ad;
    'brush=0' + sLineBreak + // &#xe3ae;
    'bubble_chart=0' + sLineBreak + // &#xe6dd;
    'bug_report=0' + sLineBreak + // &#xe868;
    'build=0' + sLineBreak + // &#xe869;
    'burst_mode=0' + sLineBreak + // &#xe43c;
    'business=0' + sLineBreak + // &#xe0af;
    'business_center=0' + sLineBreak + // &#xeb3f;
    'cake=0' + sLineBreak + // &#xe7e9;
    'call_to_action=0' + sLineBreak + // &#xe06c;
    'cached=0' + sLineBreak + // &#xe86a;
    'call=0' + sLineBreak + // &#xe0b0;
    'call_end=0' + sLineBreak + // &#xe0b1;
    'call_made=0' + sLineBreak + // &#xe0b2;
    'call_merge=0' + sLineBreak + // &#xe0b3;
    'call_missed=0' + sLineBreak + // &#xe0b4;
    'call_missed_outgoing=0' + sLineBreak + // &#xe0e4;
    'call_received=0' + sLineBreak + // &#xe0b5;
    'call_split=0' + sLineBreak + // &#xe0b6;
    'camera=0' + sLineBreak + // &#xe3af;
    'camera_alt=0' + sLineBreak + // &#xe3b0;
    'camera_enhance=0' + sLineBreak + // &#xe8fc;
    'camera_front=0' + sLineBreak + // &#xe3b1;
    'camera_rear=0' + sLineBreak + // &#xe3b2;
    'camera_roll=0' + sLineBreak + // &#xe3b3;
    'cancel=0' + sLineBreak + // &#xe5c9;
    'card_giftcard=0' + sLineBreak + // &#xe8f6;
    'card_membership=0' + sLineBreak + // &#xe8f7;
    'card_travel=0' + sLineBreak + // &#xe8f8;
    'casino=0' + sLineBreak + // &#xeb40;
    'cast=0' + sLineBreak + // &#xe307;
    'cast_connected=0' + sLineBreak + // &#xe308;
    'center_focus_strong=0' + sLineBreak + // &#xe3b4;
    'center_focus_weak=0' + sLineBreak + // &#xe3b5;
    'change_history=0' + sLineBreak + // &#xe86b;
    'chat=0' + sLineBreak + // &#xe0b7;
    'chat_bubble=0' + sLineBreak + // &#xe0ca;
    'chat_bubble_outline=0' + sLineBreak + // &#xe0cb;
    'check=0' + sLineBreak + // &#xe5ca;
    'check_box=0' + sLineBreak + // &#xe834;
    'check_box_outline_blank=0' + sLineBreak + // &#xe835;
    'check_circle=0' + sLineBreak + // &#xe86c;
    'chevron_left=0' + sLineBreak + // &#xe5cb;
    'chevron_right=0' + sLineBreak + // &#xe5cc;
    'child_care=0' + sLineBreak + // &#xeb41;
    'child_friendly=0' + sLineBreak + // &#xeb42;
    'chrome_reader_mode=0' + sLineBreak + // &#xe86d;
    'class=0' + sLineBreak + // &#xe86f;
    'clear=0' + sLineBreak + // &#xe14c;
    'clear_all=0' + sLineBreak + // &#xe0b8;
    'close=0' + sLineBreak + // &#xe5cd;
    'closed_caption=0' + sLineBreak + // &#xe01c;
    'cloud=0' + sLineBreak + // &#xe2bd;
    'cloud_circle=0' + sLineBreak + // &#xe2be;
    'cloud_done=0' + sLineBreak + // &#xe2bf;
    'cloud_download=0' + sLineBreak + // &#xe2c0;
    'cloud_off=0' + sLineBreak + // &#xe2c1;
    'cloud_queue=0' + sLineBreak + // &#xe2c2;
    'cloud_upload=0' + sLineBreak + // &#xe2c3;
    'code=0' + sLineBreak + // &#xe86f;
    'collections=0' + sLineBreak + // &#xe3b6;
    'collections_bookmark=0' + sLineBreak + // &#xe431;
    'color_lens=0' + sLineBreak + // &#xe3b7;
    'colorize=0' + sLineBreak + // &#xe3b8;
    'comment=0' + sLineBreak + // &#xe0b9;
    'compare=0' + sLineBreak + // &#xe3b9;
    'compare_arrows=0' + sLineBreak + // &#xe915;
    'computer=0' + sLineBreak + // &#xe30a;
    'confirmation_number=0' + sLineBreak + // &#xe638;
    'contact_phone=0' + sLineBreak + // &#xe0cf;
    'contact_mail=0' + sLineBreak + // &#xe0d0;
    'contacts=0' + sLineBreak + // &#xe0ba;
    'content_copy=0' + sLineBreak + // &#xe14d;
    'content_cut=0' + sLineBreak + // &#xe14e;
    'content_paste=0' + sLineBreak + // &#xe14f;
    'control_point=0' + sLineBreak + // &#xe3ba;
    'control_point_duplicate=0' + sLineBreak; // &#xe3bb;
  GOOGLE_ICONS3 =
    'copyright=0' + sLineBreak + // &#xe90c;
    'create=0' + sLineBreak + // &#xe150;
    'create_new_folder=0' + sLineBreak + // &#xe2cc;
    'credit_card=0' + sLineBreak + // &#xe870;
    'crop=0' + sLineBreak + // &#xe3be;
    'crop_16_9=0' + sLineBreak + // &#xe3bc;
    'crop_3_2=0' + sLineBreak + // &#xe3bd;
    'crop_5_4=0' + sLineBreak + // &#xe3bf;
    'crop_7_5=0' + sLineBreak + // &#xe3c0;
    'crop_din=0' + sLineBreak + // &#xe3c1;
    'crop_free=0' + sLineBreak + // &#xe3c2;
    'crop_landscape=0' + sLineBreak + // &#xe3c3;
    'crop_original=0' + sLineBreak + // &#xe3c4;
    'crop_portrait=0' + sLineBreak + // &#xe3c5;
    'crop_square=0' + sLineBreak + // &#xe3c6;
    'dashboard=0' + sLineBreak + // &#xe871;
    'date_range=0' + sLineBreak + // &#xe916;
    'data_usage=0' + sLineBreak + // &#xe1af;
    'dehaze=0' + sLineBreak + // &#xe3c7;
    'delete=0' + sLineBreak + // &#xe872;
    'delete_forever=0' + sLineBreak + // &#xe92b;
    'delete_sweep=0' + sLineBreak + // &#xe16c;
    'description=0' + sLineBreak + // &#xe873;
    'desktop_mac=0' + sLineBreak + // &#xe30b;
    'desktop_windows=0' + sLineBreak + // &#xe30c;
    'developer_board=0' + sLineBreak + // &#xe30d;
    'developer_mode=0' + sLineBreak + // &#xe1b0;
    'details=0' + sLineBreak + // &#xe3c8;
    'device_hub=0' + sLineBreak + // &#xe335;
    'devices_other=0' + sLineBreak + // &#xe337;
    'devices=0' + sLineBreak + // &#xe1b1;
    'dialer_sip=0' + sLineBreak + // &#xe0bb;
    'dialpad=0' + sLineBreak + // &#xe0bc;
    'directions=0' + sLineBreak + // &#xe52e;
    'directions_bike=0' + sLineBreak + // &#xe52f;
    'directions_boat=0' + sLineBreak + // &#xe532;
    'directions_bus=0' + sLineBreak + // &#xe530;
    'directions_car=0' + sLineBreak + // &#xe531;
    'directions_railway=0' + sLineBreak + // &#xe534;
    'directions_run=0' + sLineBreak + // &#xe566;
    'directions_subway=0' + sLineBreak + // &#xe533;
    'directions_transit=0' + sLineBreak + // &#xe535;
    'directions_walk=0' + sLineBreak + // &#xe536;
    'disc_full=0' + sLineBreak + // &#xe610;
    'dns=0' + sLineBreak + // &#xe875;
    'do_not_disturb=0' + sLineBreak + // &#xe612;
    'do_not_disturb_alt=0' + sLineBreak + // &#xe611;
    'do_not_disturb_off=0' + sLineBreak + // &#xe643;
    'do_not_disturb_on=0' + sLineBreak + // &#xe644;
    'dock=0' + sLineBreak + // &#xe30e;
    'domain=0' + sLineBreak + // &#xe7ee;
    'done=0' + sLineBreak + // &#xe876;
    'done_all=0' + sLineBreak + // &#xe877;
    'donut_large=0' + sLineBreak + // &#xe917;
    'donut_small=0' + sLineBreak + // &#xe918;
    'drafts=0' + sLineBreak + // &#xe151;
    'drag_handle=0' + sLineBreak + // &#xe25d;
    'drive_eta=0' + sLineBreak + // &#xe613;
    'dvr=0' + sLineBreak + // &#xe1b2;
    'edit=0' + sLineBreak + // &#xe3c9;
    'edit_location=0' + sLineBreak + // &#xe568;
    'eject=0' + sLineBreak + // &#xe8fb;
    'email=0' + sLineBreak + // &#xe0be;
    'enhanced_encryption=0' + sLineBreak + // &#xe63f;
    'equalizer=0' + sLineBreak + // &#xe01d;
    'error=0' + sLineBreak + // &#xe000;
    'error_outline=0' + sLineBreak + // &#xe001;
    'euro_symbol=0' + sLineBreak + // &#xe926;
    'ev_station=0' + sLineBreak + // &#xe56d;
    'event=0' + sLineBreak + // &#xe878;
    'event_available=0' + sLineBreak + // &#xe614;
    'event_busy=0' + sLineBreak + // &#xe615;
    'event_note=0' + sLineBreak + // &#xe616;
    'event_seat=0' + sLineBreak + // &#xe903;
    'exit_to_app=0' + sLineBreak + // &#xe879;
    'expand_less=0' + sLineBreak + // &#xe5ce;
    'expand_more=0' + sLineBreak + // &#xe5cf;
    'explicit=0' + sLineBreak + // &#xe01e;
    'explore=0' + sLineBreak + // &#xe87a;
    'exposure=0' + sLineBreak + // &#xe3ca;
    'exposure_neg_1=0' + sLineBreak + // &#xe3cb;
    'exposure_neg_2=0' + sLineBreak + // &#xe3cc;
    'exposure_plus_1=0' + sLineBreak + // &#xe3cd;
    'exposure_plus_2=0' + sLineBreak + // &#xe3ce;
    'exposure_zero=0' + sLineBreak + // &#xe3cf;
    'extension=0' + sLineBreak + // &#xe87b;
    'face=0' + sLineBreak + // &#xe87c;
    'fast_forward=0' + sLineBreak + // &#xe01f;
    'fast_rewind=0' + sLineBreak + // &#xe020;
    'favorite=0' + sLineBreak + // &#xe87d;
    'favorite_border=0' + sLineBreak + // &#xe87e;
    'featured_play_list=0' + sLineBreak + // &#xe06d;
    'featured_video=0' + sLineBreak + // &#xe06e;
    'feedback=0' + sLineBreak + // &#xe87f;
    'fiber_dvr=0' + sLineBreak + // &#xe05d;
    'fiber_manual_record=0' + sLineBreak + // &#xe061;
    'fiber_new=0' + sLineBreak + // &#xe05e;
    'fiber_pin=0' + sLineBreak + // &#xe06a;
    'fiber_smart_record=0' + sLineBreak; // &#xe062;
  GOOGLE_ICONS4 =
    'file_download=0' + sLineBreak + // &#xe2c4;
    'file_upload=0' + sLineBreak + // &#xe2c6;
    'filter=0' + sLineBreak + // &#xe3d3;
    'filter_1=0' + sLineBreak + // &#xe3d0;
    'filter_2=0' + sLineBreak + // &#xe3d1;
    'filter_3=0' + sLineBreak + // &#xe3d2;
    'filter_4=0' + sLineBreak + // &#xe3d4;
    'filter_5=0' + sLineBreak + // &#xe3d5;
    'filter_6=0' + sLineBreak + // &#xe3d6;
    'filter_7=0' + sLineBreak + // &#xe3d7;
    'filter_8=0' + sLineBreak + // &#xe3d8;
    'filter_9=0' + sLineBreak + // &#xe3d9;
    'filter_9_plus=0' + sLineBreak + // &#xe3da;
    'filter_b_and_w=0' + sLineBreak + // &#xe3db;
    'filter_center_focus=0' + sLineBreak + // &#xe3dc;
    'filter_drama=0' + sLineBreak + // &#xe3dd;
    'filter_frames=0' + sLineBreak + // &#xe3de;
    'filter_hdr=0' + sLineBreak + // &#xe3df;
    'filter_list=0' + sLineBreak + // &#xe152;
    'filter_none=0' + sLineBreak + // &#xe3e0;
    'filter_tilt_shift=0' + sLineBreak + // &#xe3e2;
    'filter_vintage=0' + sLineBreak + // &#xe3e3;
    'find_in_page=0' + sLineBreak + // &#xe880;
    'find_replace=0' + sLineBreak + // &#xe881;
    'fingerprint=0' + sLineBreak + // &#xe90d;
    'first_page=0' + sLineBreak + // &#xe5dc;
    'fitness_center=0' + sLineBreak + // &#xeb43;
    'flag=0' + sLineBreak + // &#xe153;
    'flare=0' + sLineBreak + // &#xe3e4;
    'flash_auto=0' + sLineBreak + // &#xe3e5;
    'flash_off=0' + sLineBreak + // &#xe3e6;
    'flash_on=0' + sLineBreak + // &#xe3e7;
    'flight=0' + sLineBreak + // &#xe539;
    'flight_land=0' + sLineBreak + // &#xe904;
    'flight_takeoff=0' + sLineBreak + // &#xe905;
    'flip=0' + sLineBreak + // &#xe3e8;
    'flip_to_back=0' + sLineBreak + // &#;
    'flip_to_front=0' + sLineBreak + // &#xe883;
    'folder=0' + sLineBreak + // &#xe2c7;
    'folder_open=0' + sLineBreak + // &#xe2c8;
    'folder_shared=0' + sLineBreak + // &#xe2c9;
    'folder_special=0' + sLineBreak + // &#xe617;
    'font_download=0' + sLineBreak + // &#xe167;
    'format_align_center=0' + sLineBreak + // &#xe234;
    'format_align_justify=0' + sLineBreak + // &#xe235;
    'format_align_left=0' + sLineBreak + // &#xe236;
    'format_align_right=0' + sLineBreak + // &#xe237;
    'format_bold=0' + sLineBreak + // &#xe238;
    'format_clear=0' + sLineBreak + // &#xe239;
    'format_color_fill=0' + sLineBreak + // &#xe23a;
    'format_color_reset=0' + sLineBreak + // &#xe23b;
    'format_color_text=0' + sLineBreak + // &#xe23c;
    'format_indent_decrease=0' + sLineBreak + // &#xe23d;
    'format_indent_increase=0' + sLineBreak + // &#xe23e;
    'format_italic=0' + sLineBreak + // &#xe23f;
    'format_line_spacing=0' + sLineBreak + // &#xe240;
    'format_list_bulleted=0' + sLineBreak + // &#xe241;
    'format_list_numbered=0' + sLineBreak + // &#xe242;
    'format_paint=0' + sLineBreak + // &#xe243;
    'format_quote=0' + sLineBreak + // &#xe244;
    'format_shapes=0' + sLineBreak + // &#xe25e;
    'format_size=0' + sLineBreak + // &#xe245;
    'format_strikethrough=0' + sLineBreak + // &#xe246;
    'format_textdirection_l_to_r=0' + sLineBreak + // &#xe247;
    'format_textdirection_r_to_l=0' + sLineBreak + // &#xe248;
    'format_underlined=0' + sLineBreak + // &#xe249;
    'forum=0' + sLineBreak + // &#xe0bf;
    'forward=0' + sLineBreak + // &#xe154;
    'forward_10=0' + sLineBreak + // &#xe056;
    'forward_30=0' + sLineBreak + // &#xe057;
    'forward_5=0' + sLineBreak + // &#xe058;
    'free_breakfast=0' + sLineBreak + // &#xeb44;
    'fullscreen=0' + sLineBreak + // &#xe5d0;
    'fullscreen_exit=0' + sLineBreak + // &#xe5d1;
    'functions=0' + sLineBreak + // &#xe24a;
    'g_translate=0' + sLineBreak + // &#xe927;
    'gamepad=0' + sLineBreak + // &#xe30f;
    'games=0' + sLineBreak + // &#xe021;
    'gavel=0' + sLineBreak + // &#xe90e;
    'gesture=0' + sLineBreak + // &#xe155;
    'get_app=0' + sLineBreak + // &#xe884;
    'gif=0' + sLineBreak + // &#xe908;
    'golf_course=0' + sLineBreak + // &#xeb45;
    'gps_fixed=0' + sLineBreak + // &#xe1b3;
    'gps_not_fixed=0' + sLineBreak + // &#xe1b4;
    'gps_off=0' + sLineBreak + // &#xe1b5;
    'grade=0' + sLineBreak + // &#xe885;
    'gradient=0' + sLineBreak + // &#xe3e9;
    'grain=0' + sLineBreak + // &#xe3ea;
    'graphic_eq=0' + sLineBreak + // &#xe1b8;
    'grid_off=0' + sLineBreak + // &#xe3eb;
    'grid_on=0' + sLineBreak + // &#xe3ec;
    'group=0' + sLineBreak + // &#xe7ef;
    'group_add=0' + sLineBreak + // &#xe7f0;
    'group_work=0' + sLineBreak + // &#xe886;
    'hd=0' + sLineBreak + // &#xe052;
    'hdr_off=0' + sLineBreak + // &#xe3ed;
    'hdr_on=0' + sLineBreak + // &#xe3ee;
    'hdr_strong=0' + sLineBreak; // &#xe3f1;
  GOOGLE_ICONS5 =
    'hdr_weak=0' + sLineBreak + // &#xe3f2;
    'headset=0' + sLineBreak + // &#xe310;
    'headset_mic=0' + sLineBreak + // &#xe311;
    'healing=0' + sLineBreak + // &#xe3f3;
    'hearing=0' + sLineBreak + // &#xe023;
    'help=0' + sLineBreak + // &#xe887;
    'help_outline=0' + sLineBreak + // &#xe8fd;
    'high_quality=0' + sLineBreak + // &#xe024;
    'highlight=0' + sLineBreak + // &#xe25f;
    'highlight_off=0' + sLineBreak + // &#xe888;
    'history=0' + sLineBreak + // &#xe889;
    'home=0' + sLineBreak + // &#xe88a;
    'hot_tub=0' + sLineBreak + // &#xeb46;
    'hotel=0' + sLineBreak + // &#xe53a;
    'hourglass_empty=0' + sLineBreak + // &#xe88b;
    'hourglass_full=0' + sLineBreak + // &#xe88c;
    'http=0' + sLineBreak + // &#xe902;
    'https=0' + sLineBreak + // &#xe88d;
    'image=0' + sLineBreak + // &#xe3f4;
    'image_aspect_ratio=0' + sLineBreak + // &#xe3f5;
    'import_contacts=0' + sLineBreak + // &#xe0e0;
    'import_export=0' + sLineBreak + // &#xe0c3;
    'important_devices=0' + sLineBreak + // &#xe912;
    'inbox=0' + sLineBreak + // &#xe156;
    'indeterminate_check_box=0' + sLineBreak + // &#xe909;
    'info=0' + sLineBreak + // &#xe88e;
    'info_outline=0' + sLineBreak + // &#xe88f;
    'input=0' + sLineBreak + // &#xe890;
    'insert_chart=0' + sLineBreak + // &#xe24b;
    'insert_comment=0' + sLineBreak + // &#xe24c;
    'insert_drive_file=0' + sLineBreak + // &#xe24d;
    'insert_emoticon=0' + sLineBreak + // &#xe24e;
    'insert_invitation=0' + sLineBreak + // &#xe24f;
    'insert_link=0' + sLineBreak + // &#xe250;
    'insert_photo=0' + sLineBreak + // &#xe251;
    'invert_colors=0' + sLineBreak + // &#xe891;
    'invert_colors_off=0' + sLineBreak + // &#xe0c4;
    'iso=0' + sLineBreak + // &#xe3f6;
    'keyboard=0' + sLineBreak + // &#xe312;
    'keyboard_arrow_down=0' + sLineBreak + // &#xe313;
    'keyboard_arrow_left=0' + sLineBreak + // &#xe314;
    'keyboard_arrow_right=0' + sLineBreak + // &#xe315;
    'keyboard_arrow_up=0' + sLineBreak + // &#xe316;
    'keyboard_backspace=0' + sLineBreak + // &#xe317;
    'keyboard_capslock=0' + sLineBreak + // &#xe318;
    'keyboard_hide=0' + sLineBreak + // &#xe31a;
    'keyboard_return=0' + sLineBreak + // &#xe31b;
    'keyboard_tab=0' + sLineBreak + // &#xe31c;
    'keyboard_voice=0' + sLineBreak + // &#xe31d;
    'kitchen=0' + sLineBreak + // &#xeb47;
    'label=0' + sLineBreak + // &#xe892;
    'label_outline=0' + sLineBreak + // &#xe893;
    'landscape=0' + sLineBreak + // &#xe3f7;
    'language=0' + sLineBreak + // &#xe894;
    'laptop=0' + sLineBreak + // &#xe31e;
    'laptop_chromebook=0' + sLineBreak + // &#xe31f;
    'laptop_mac=0' + sLineBreak + // &#xe320;
    'laptop_windows=0' + sLineBreak + // &#xe321;
    'last_page=0' + sLineBreak + // &#xe5dd;
    'launch=0' + sLineBreak + // &#xe895;
    'layers=0' + sLineBreak + // &#xe53b;
    'layers_clear=0' + sLineBreak + // &#xe53c;
    'leak_add=0' + sLineBreak + // &#xe3f8;
    'leak_remove=0' + sLineBreak + // &#xe3f9;
    'lens=0' + sLineBreak + // &#xe3fa;
    'library_add=0' + sLineBreak + // &#xe02e;
    'library_books=0' + sLineBreak + // &#xe02f;
    'library_music=0' + sLineBreak + // &#xe030;
    'lightbulb_outline=0' + sLineBreak + // &#xe90f;
    'line_style=0' + sLineBreak + // &#xe919;
    'line_weight=0' + sLineBreak + // &#xe91a;
    'linear_scale=0' + sLineBreak + // &#xe260;
    'link=0' + sLineBreak + // &#xe157;
    'linked_camera=0' + sLineBreak + // &#xe438;
    'list=0' + sLineBreak + // &#xe896;
    'live_help=0' + sLineBreak + // &#xe0c6;
    'live_tv=0' + sLineBreak + // &#xe639;
    'local_activity=0' + sLineBreak + // &#xe53f;
    'local_airport=0' + sLineBreak + // &#xe53d;
    'local_atm=0' + sLineBreak + // &#xe53e;
    'local_bar=0' + sLineBreak + // &#xe540;
    'local_cafe=0' + sLineBreak + // &#xe541;
    'local_car_wash=0' + sLineBreak + // &#xe542;
    'local_convenience_store=0' + sLineBreak + // &#xe543;
    'local_dining=0' + sLineBreak + // &#xe556;
    'local_drink=0' + sLineBreak + // &#xe544;
    'local_florist=0' + sLineBreak + // &#xe545;
    'local_gas_station=0' + sLineBreak + // &#xe546;
    'local_grocery_store=0' + sLineBreak + // &#xe547;
    'local_hospital=0' + sLineBreak + // &#xe548;
    'local_hotel=0' + sLineBreak + // &#xe549;
    'local_laundry_service=0' + sLineBreak + // &#xe54a;
    'local_library=0' + sLineBreak + // &#xe54b;
    'local_mall=0' + sLineBreak + // &#xe54c;
    'local_movies=0' + sLineBreak + // &#xe54d;
    'local_offer=0' + sLineBreak + // &#xe54e;
    'local_parking=0' + sLineBreak + // &#xe54f;
    'local_pharmacy=0' + sLineBreak + // &#xe550;
    'local_phone=0' + sLineBreak; // &#xe551;
  GOOGLE_ICONS6 =
    'local_pizza=0' + sLineBreak + // &#xe552;
    'local_play=0' + sLineBreak + // &#xe553;
    'local_post_office=0' + sLineBreak + // &#xe554;
    'local_printshop=0' + sLineBreak + // &#xe555;
    'local_see=0' + sLineBreak + // &#xe557;
    'local_shipping=0' + sLineBreak + // &#xe558;
    'local_taxi=0' + sLineBreak + // &#xe559;
    'location_city=0' + sLineBreak + // &#xe7f1;
    'location_disabled=0' + sLineBreak + // &#xe1b6;
    'location_off=0' + sLineBreak + // &#xe0c7;
    'location_on=0' + sLineBreak + // &#xe0c8;
    'location_searching=0' + sLineBreak + // &#xe1b7;
    'lock=0' + sLineBreak + // &#xe897;
    'lock_open=0' + sLineBreak + // &#xe898;
    'lock_outline=0' + sLineBreak + // &#xe899;
    'looks=0' + sLineBreak + // &#xe3fc;
    'looks_3=0' + sLineBreak + // &#xe3fb;
    'looks_4=0' + sLineBreak + // &#xe3fd;
    'looks_5=0' + sLineBreak + // &#xe3fe;
    'looks_6=0' + sLineBreak + // &#xe3ff;
    'looks_one=0' + sLineBreak + // &#xe400;
    'looks_two=0' + sLineBreak + // &#xe401;
    'loop=0' + sLineBreak + // &#xe028;
    'loupe=0' + sLineBreak + // &#xe402;
    'loyalty=0' + sLineBreak + // &#xe89a;
    'low_priority=0' + sLineBreak + // &#xe16d;
    'mail=0' + sLineBreak + // &#xe158;
    'mail_outline=0' + sLineBreak + // &#xe0e1;
    'map=0' + sLineBreak + // &#xe55b;
    'markunread=0' + sLineBreak + // &#xe159;
    'markunread_mailbox=0' + sLineBreak + // &#xe89b;
    'memory=0' + sLineBreak + // &#xe322;
    'menu=0' + sLineBreak + // &#xe5d2;
    'merge_type=0' + sLineBreak + // &#xe252;
    'message=0' + sLineBreak + // &#xe0c9;
    'messenger=0' + sLineBreak + //
    'mic=0' + sLineBreak + // &#xe029;
    'mic_none=0' + sLineBreak + // &#xe02a;
    'mic_off=0' + sLineBreak + // &#xe02b;
    'mms=0' + sLineBreak + // &#xe618;
    'mode_comment=0' + sLineBreak + // &#xe253;
    'mode_edit=0' + sLineBreak + // &#xe254;
    'monetization_on=0' + sLineBreak + // &#xe263;
    'money_off=0' + sLineBreak + // &#xe25c;
    'monochrome_photos=0' + sLineBreak + // &#xe403;
    'mood=0' + sLineBreak + // &#xe7f2;
    'mood_bad=0' + sLineBreak + // &#xe7f3;
    'more=0' + sLineBreak + // &#xe619;
    'more_horiz=0' + sLineBreak + // &#xe5d3;
    'more_vert=0' + sLineBreak + // &#xe5d4;
    'motorcycle=0' + sLineBreak + // &#xe91b;
    'mouse=0' + sLineBreak + // &#xe91b;
    'move_to_inbox=0' + sLineBreak + // &#xe168;
    'movie=0' + sLineBreak + // &#xe02c;
    'movie_creation=0' + sLineBreak + // &#xe404;
    'movie_filter=0' + sLineBreak + // &#xe43a;
    'multiline_chart=0' + sLineBreak + // &#xe6df;
    'music_note=0' + sLineBreak + // &#xe405;
    'music_video=0' + sLineBreak + // &#xe063;
    'my_location=0' + sLineBreak + // &#xe55c;
    'nature=0' + sLineBreak + // &#xe406;
    'nature_people=0' + sLineBreak + // &#xe407;
    'navigate_before=0' + sLineBreak + // &#xe408;
    'navigate_next=0' + sLineBreak + // &#xe409;
    'navigation=0' + sLineBreak + // &#xe55d;
    'near_me=0' + sLineBreak + // &#xe569;
    'network_check=0' + sLineBreak + // &#xe640;
    'network_locked=0' + sLineBreak + // &#xe61a;
    'network_cell=0' + sLineBreak + // &#xe1b9;
    'network_wifi=0' + sLineBreak + // &#xe1ba;
    'new_releases=0' + sLineBreak + // &#xe031;
    'next_week=0' + sLineBreak + // &#xe16a;
    'nfc=0' + sLineBreak + // &#xe1bb;
    'no_encryption=0' + sLineBreak + // &#xe641;
    'no_sim=0' + sLineBreak + // &#xe0cc;
    'not_interested=0' + sLineBreak + // &#xe033;
    'note=0' + sLineBreak + // &#xe06f;
    'note_add=0' + sLineBreak + // &#xe89c;
    'notifications=0' + sLineBreak + // &#xe7f4;
    'notifications_none=0' + sLineBreak + // &#xe7f5;
    'notifications_off=0' + sLineBreak + // &#xe7f6;
    'notifications_active=0' + sLineBreak + // &#xe7f7;
    'notifications_paused=0' + sLineBreak + // &#xe7f8;
    'offline_pin=0' + sLineBreak + // &#xe90a;
    'ondemand_video=0' + sLineBreak + // &#xe63a;
    'opacity=0' + sLineBreak + // &#xe91c;
    'open_in_browser=0' + sLineBreak + // &#xe89d;
    'open_in_new=0' + sLineBreak + // &#xe89e;
    'open_with=0' + sLineBreak + // &#xe89f;
    'pages=0' + sLineBreak + // &#xe7f9;
    'pageview=0' + sLineBreak + // &#xe8a0;
    'palette=0' + sLineBreak + // &#xe40a;
    'pan_tool=0' + sLineBreak + // &#xe925;
    'panorama=0' + sLineBreak + // &#xe40b;
    'panorama_fish_eye=0' + sLineBreak + // &#xe40c;
    'panorama_horizontal=0' + sLineBreak + // &#xe40d;
    'panorama_vertical=0' + sLineBreak + // &#xe40e;
    'panorama_wide_angle=0' + sLineBreak + // &#xe40f;
    'party_mode=0' + sLineBreak; // &#xe7fa;
  GOOGLE_ICONS7 =
    'pause=0' + sLineBreak + // &#xe034;
    'pause_circle_filled=0' + sLineBreak + // &#xe035;
    'pause_circle_outline=0' + sLineBreak + // &#xe036;
    'payment=0' + sLineBreak + // &#xe8a1;
    'people=0' + sLineBreak + // &#xe7fb;
    'people_outline=0' + sLineBreak + // &#xe7fc;
    'perm_camera_mic=0' + sLineBreak + // &#xe8a2;
    'perm_contact_calendar=0' + sLineBreak + // &#xe8a3;
    'perm_data_setting=0' + sLineBreak + // &#xe8a4;
    'perm_device_information=0' + sLineBreak + // &#xe8a5;
    'perm_identity=0' + sLineBreak + // &#xe8a6;
    'perm_media=0' + sLineBreak + // &#xe8a7;
    'perm_phone_msg=0' + sLineBreak + // &#xe8a8;
    'perm_scan_wifi=0' + sLineBreak + // &#xe8a9;
    'person=0' + sLineBreak + // &#xe7fd;
    'person_add=0' + sLineBreak + // &#xe7fe;
    'person_outline=0' + sLineBreak + // &#xe7ff;
    'person_pin=0' + sLineBreak + // &#xe55a;
    'person_pin_circle=0' + sLineBreak + // &#xe56a;
    'personal_video=0' + sLineBreak + // &#xe63b;
    'pets=0' + sLineBreak + // &#xe91d;
    'phone=0' + sLineBreak + // &#xe0cd;
    'phone_android=0' + sLineBreak + // &#xe324;
    'phone_bluetooth_speaker=0' + sLineBreak + // &#xe61b;
    'phone_forwarded=0' + sLineBreak + // &#xe61c;
    'phone_in_talk=0' + sLineBreak + // &#xe61d;
    'phone_iphone=0' + sLineBreak + // &#xe325;
    'phone_locked=0' + sLineBreak + // &#xe61e;
    'phone_missed=0' + sLineBreak + // &#xe61f;
    'phone_paused=0' + sLineBreak + // &#xe620;
    'phonelink=0' + sLineBreak + // &#xe326;
    'phonelink_erase=0' + sLineBreak + // &#xe0db;
    'phonelink_lock=0' + sLineBreak + // &#xe0dc;
    'phonelink_off=0' + sLineBreak + // &#xe327;
    'phonelink_ring=0' + sLineBreak + // &#xe0dd;
    'phonelink_setup=0' + sLineBreak + // &#xe0de;
    'photo=0' + sLineBreak + // &#xe410;
    'photo_album=0' + sLineBreak + // &#xe411;
    'photo_camera=0' + sLineBreak + // &#xe412;
    'photo_filter=0' + sLineBreak + // &#xe43b;
    'photo_library=0' + sLineBreak + // &#xe413;
    'photo_size_select_actual=0' + sLineBreak + // &#xe432;
    'photo_size_select_large=0' + sLineBreak + // &#xe433;
    'photo_size_select_small=0' + sLineBreak + // &#xe434;
    'picture_as_pdf=0' + sLineBreak + // &#xe415;
    'picture_in_picture=0' + sLineBreak + // &#xe8aa;
    'picture_in_picture_alt=0' + sLineBreak + // &#xe911;
    'pie_chart=0' + sLineBreak + // &#xe6c4;
    'pie_chart_outlined=0' + sLineBreak + // &#xe6c5;
    'pin_drop=0' + sLineBreak + // &#xe55e;
    'place=0' + sLineBreak + // &#xe55f;
    'play_arrow=0' + sLineBreak + // &#xe037;
    'play_circle_filled=0' + sLineBreak + // &#xe038;
    'play_circle_outline=0' + sLineBreak + // &#xe039;
    'play_for_work=0' + sLineBreak + // &#xe906;
    'playlist_add=0' + sLineBreak + // &#xe03b;
    'playlist_add_check=0' + sLineBreak + // &#xe065;
    'playlist_play=0' + sLineBreak + // &#xe05f;
    'plus_one=0' + sLineBreak + // &#xe800;
    'poll=0' + sLineBreak + // &#xe801;
    'polymer=0' + sLineBreak + // &#xe8ab;
    'pool=0' + sLineBreak + // &#xeb48;
    'portable_wifi_off=0' + sLineBreak + // &#xe0ce;
    'portrait=0' + sLineBreak + // &#xe416;
    'power=0' + sLineBreak + // &#xe63c;
    'power_input=0' + sLineBreak + // &#xe336;
    'power_settings_new=0' + sLineBreak + // &#xe8ac;
    'pregnant_woman=0' + sLineBreak + // &#xe91e;
    'present_to_all=0' + sLineBreak + // &#xe0df;
    'publish=0' + sLineBreak + // &#xe255;
    'print=0' + sLineBreak + // &#xe8ad;
    'priority_high=0' + sLineBreak + // &#xe645;
    'public=0' + sLineBreak + // &#xe80b;
    'query_builder=0' + sLineBreak + // &#xe8ae;
    'question_answer=0' + sLineBreak + // &#xe8af;
    'queue=0' + sLineBreak + // &#xe03c;
    'queue_music=0' + sLineBreak + // &#xe03d;
    'queue_play_next=0' + sLineBreak + // &#xe066;
    'radio=0' + sLineBreak + // &#xe03e;
    'radio_button_unchecked=0' + sLineBreak + // &#xe836;
    'radio_button_checked=0' + sLineBreak + // &#xe837;
    'rate_review=0' + sLineBreak + // &#xe560;
    'receipt=0' + sLineBreak + // &#xe8b0;
    'recent_actors=0' + sLineBreak + // &#xe03f;
    'record_voice_over=0' + sLineBreak + // &#xe91f;
    'redeem=0' + sLineBreak + // &#xe8b1;
    'redo=0' + sLineBreak + // &#xe15a;
    'refresh=0' + sLineBreak + // &#xe5d5;
    'remove=0' + sLineBreak + // &#xe15b;
    'remove_circle=0' + sLineBreak + // &#xe15c;
    'remove_circle_outline=0' + sLineBreak + // &#xe15d;
    'remove_from_queue=0' + sLineBreak + // &#xe067;
    'remove_red_eye=0' + sLineBreak + // &#xe417;
    'remove_shopping_cart=0' + sLineBreak + // &#xe928;
    'reorder=0' + sLineBreak + // &#xe8fe;
    'repeat=0' + sLineBreak + // &#xe040;
    'repeat_one=0' + sLineBreak + // &#xe041;
    'replay=0' + sLineBreak + // &#xe042;
    'replay_10=0' + sLineBreak; // &#xe059;
  GOOGLE_ICONS8 =
    'replay_30=0' + sLineBreak + // &#xe05a;
    'replay_5=0' + sLineBreak + // &#xe05b;
    'reply=0' + sLineBreak + // &#xe15e;
    'reply_all=0' + sLineBreak + // &#xe15f;
    'report=0' + sLineBreak + // &#xe160;
    'report_problem=0' + sLineBreak + // &#xe8b2;
    'restaurant=0' + sLineBreak + // &#xe56c;
    'restaurant_menu=0' + sLineBreak + // &#xe561;
    'restore=0' + sLineBreak + // &#xe8b3;
    'restore_page=0' + sLineBreak + // &#xe929;
    'ring_volume=0' + sLineBreak + // &#xe0d1;
    'room=0' + sLineBreak + // &#xe8b4;
    'room_service=0' + sLineBreak + // &#xeb49;
    'rotate_90_degrees_ccw=0' + sLineBreak + // &#xe418;
    'rotate_left=0' + sLineBreak + // &#xe419;
    'rotate_right=0' + sLineBreak + // &#xe41a;
    'rounded_corner=0' + sLineBreak + // &#xe920;
    'router=0' + sLineBreak + // &#xe328;
    'rowing=0' + sLineBreak + // &#xe921;
    'rss_feed=0' + sLineBreak + // &#xe0e5;
    'rv_hookup=0' + sLineBreak + // &#xe642;
    'satellite=0' + sLineBreak + // &#xe562;
    'save=0' + sLineBreak + // &#xe161;
    'scanner=0' + sLineBreak + // &#xe329;
    'schedule=0' + sLineBreak + // &#xe8b5;
    'school=0' + sLineBreak + // &#xe80c;
    'screen_lock_landscape=0' + sLineBreak + // &#xe1be;
    'screen_lock_portrait=0' + sLineBreak + // &#xe1bf;
    'screen_lock_rotation=0' + sLineBreak + // &#xe1c0;
    'screen_rotation=0' + sLineBreak + // &#xe1c1;
    'screen_share=0' + sLineBreak + // &#xe0e2;
    'sd_card=0' + sLineBreak + // &#xe623;
    'sd_storage=0' + sLineBreak + // &#xe1c2;
    'search=0' + sLineBreak + // &#xe8b6;
    'security=0' + sLineBreak + // &#xe32a;
    'select_all=0' + sLineBreak + // &#xe162;
    'send=0' + sLineBreak + // &#xe163;
    'sentiment_dissatisfied=0' + sLineBreak + // &#xe811;
    'sentiment_neutral=0' + sLineBreak + // &#xe812;
    'sentiment_satisfied=0' + sLineBreak + // &#xe813;
    'sentiment_very_dissatisfied=0' + sLineBreak + // &#xe814;
    'sentiment_very_satisfied=0' + sLineBreak + // &#xe815;
    'settings=0' + sLineBreak + // &#xe8b8;
    'settings_applications=0' + sLineBreak + // &#xe8b9;
    'settings_backup_restore=0' + sLineBreak + // &#xe8ba;
    'settings_bluetooth=0' + sLineBreak + // &#xe8bb;
    'settings_brightness=0' + sLineBreak + // &#xe8bd;
    'settings_cell=0' + sLineBreak + // &#xe8bc;
    'settings_ethernet=0' + sLineBreak + // &#xe8be;
    'settings_input_antenna=0' + sLineBreak + // &#xe8bf;
    'settings_input_component=0' + sLineBreak + // &#xe8c0;
    'settings_input_composite=0' + sLineBreak + // &#xe8c1;
    'settings_input_hdmi=0' + sLineBreak + // &#xe8c2;
    'settings_input_svideo=0' + sLineBreak + // &#xe8c3;
    'settings_overscan=0' + sLineBreak + // &#xe8c4;
    'settings_phone=0' + sLineBreak + // &#xe8c5;
    'settings_power=0' + sLineBreak + // &#xe8c6;
    'settings_remote=0' + sLineBreak + // &#xe8c7;
    'settings_system_daydream=0' + sLineBreak + // &#xe1c3;
    'settings_voice=0' + sLineBreak + // &#xe8c8;
    'share=0' + sLineBreak + // &#xe80d;
    'shop=0' + sLineBreak + // &#xe8c9;
    'shop_two=0' + sLineBreak + // &#xe8ca;
    'shopping_basket=0' + sLineBreak + // &#xe8cb;
    'shopping_cart=0' + sLineBreak + // &#xe8cc;
    'short_text=0' + sLineBreak + // &#xe261;
    'show_chart=0' + sLineBreak + // &#xe6e1;
    'shuffle=0' + sLineBreak + // &#xe043;
    'signal_cellular_4_bar=0' + sLineBreak + // &#xe1c8;
    'signal_cellular_connected_no_internet_4_bar=0' + sLineBreak + // &#xe1cd;
    'signal_cellular_no_sim=0' + sLineBreak + // &#xe1ce;
    'signal_cellular_null=0' + sLineBreak + // &#xe1cf;
    'signal_cellular_off=0' + sLineBreak + // &#xe1d0;
    'signal_wifi_4_bar=0' + sLineBreak + // &#xe1d8;
    'signal_wifi_4_bar_lock=0' + sLineBreak + // &#xe1d9;
    'signal_wifi_off=0' + sLineBreak + // &#xe1da;
    'sim_card=0' + sLineBreak + // &#xe32b;
    'sim_card_alert=0' + sLineBreak + // &#xe624;
    'skip_next=0' + sLineBreak + // &#xe044;
    'skip_previous=0' + sLineBreak + // &#xe045;
    'slideshow=0' + sLineBreak + // &#xe41b;
    'slow_motion_video=0' + sLineBreak + // &#xe068;
    'smartphone=0' + sLineBreak + // &#xe32c;
    'smoke_free=0' + sLineBreak + // &#xeb4a;
    'smoking_rooms=0' + sLineBreak + // &#xeb4b;
    'sms=0' + sLineBreak + // &#xe625;
    'sms_failed=0' + sLineBreak + // &#xe626;
    'snooze=0' + sLineBreak + // &#xe046;
    'sort=0' + sLineBreak + // &#xe164;
    'sort_by_alpha=0' + sLineBreak + // &#xe053;
    'spa=0' + sLineBreak + // &#xeb4c;
    'space_bar=0' + sLineBreak + // &#xe256;
    'speaker=0' + sLineBreak + // &#xe32d;
    'speaker_group=0' + sLineBreak + // &#xe32e;
    'speaker_notes=0' + sLineBreak + // &#xe8cd;
    'speaker_notes_off=0' + sLineBreak + // &#xe92a;
    'speaker_phone=0' + sLineBreak + // &#xe0d2;
    'spellcheck=0' + sLineBreak + // &#xe8ce;
    'star=0' + sLineBreak; // &#xe838;
  GOOGLE_ICONS9 =
    'star_half=0' + sLineBreak + // &#xe839;
    'star_border=0' + sLineBreak + // &#xe83a;
    'star_rate=0' + sLineBreak + // &#xe8cf;
    'stars=0' + sLineBreak + // &#xe8d0;
    'stay_current_landscape=0' + sLineBreak + // &#xe0d3;
    'stay_current_portrait=0' + sLineBreak + // &#xe0d4;
    'stay_primary_landscape=0' + sLineBreak + // &#xe0d5;
    'stay_primary_portrait=0' + sLineBreak + // &#xe0d6;
    'stop=0' + sLineBreak + // &#xe047;
    'stop_screen_share=0' + sLineBreak + // &#xe0e3;
    'storage=0' + sLineBreak + // &#xe1db;
    'store=0' + sLineBreak + // &#xe8d1;
    'store_mall_directory=0' + sLineBreak + // &#xe563;
    'streetview=0' + sLineBreak + // &#xe56e;
    'straighten=0' + sLineBreak + // &#xe41c;
    'strikethrough_s=0' + sLineBreak + // &#xe257;
    'style=0' + sLineBreak + // &#xe41d;
    'subdirectory_arrow_left=0' + sLineBreak + // &#xe5d9;
    'subdirectory_arrow_right=0' + sLineBreak + // &#xe5da;
    'subject=0' + sLineBreak + // &#xe8d2;
    'subscriptions=0' + sLineBreak + // &#xe064;
    'subtitles=0' + sLineBreak + // &#xe048;
    'subway=0' + sLineBreak + // &#xe56f;
    'supervisor_account=0' + sLineBreak + // &#xe8d3;
    'surround_sound=0' + sLineBreak + // &#xe049;
    'swap_calls=0' + sLineBreak + // &#xe0d7;
    'swap_horiz=0' + sLineBreak + // &#xe8d4;
    'swap_vert=0' + sLineBreak + // &#xe8d5;
    'swap_vertical_circle=0' + sLineBreak + // &#xe8d6;
    'switch_camera=0' + sLineBreak + // &#xe41e;
    'switch_video=0' + sLineBreak + // &#xe41f;
    'sync=0' + sLineBreak + // &#xe627;
    'sync_disabled=0' + sLineBreak + // &#xe628;
    'sync_problem=0' + sLineBreak + // &#xe629;
    'system_update=0' + sLineBreak + // &#xe62a;
    'system_update_alt=0' + sLineBreak + // &#xe8d7;
    'tab=0' + sLineBreak + // &#xe8d8;
    'tab_unselected=0' + sLineBreak + // &#xe8d9;
    'tablet=0' + sLineBreak + // &#xe32f;
    'tablet_android=0' + sLineBreak + // &#xe330;
    'tablet_mac=0' + sLineBreak + // &#xe331;
    'tag_faces=0' + sLineBreak + // &#xe420;
    'tap_and_play=0' + sLineBreak + // &#xe62b;
    'terrain=0' + sLineBreak + // &#xe564;
    'text_fields=0' + sLineBreak + // &#xe262;
    'text_format=0' + sLineBreak + // &#xe165;
    'textsms=0' + sLineBreak + // &#xe0d8;
    'texture=0' + sLineBreak + // &#xe421;
    'theaters=0' + sLineBreak + // &#xe8da;
    'thumb_down=0' + sLineBreak + // &#xe8db;
    'thumb_up=0' + sLineBreak + // &#xe8dc;
    'thumbs_up_down=0' + sLineBreak + // &#xe8dd;
    'timelapse=0' + sLineBreak + // &#xe422;
    'timer_10=0' + sLineBreak + // &#xe423;
    'time_to_leave=0' + sLineBreak + // &#xe62c;
    'timer=0' + sLineBreak + // &#xe425;
    'timer_3=0' + sLineBreak + // &#xe424;
    'timer_off=0' + sLineBreak + // &#xe426;
    'title=0' + sLineBreak + // &#xe264;
    'toc=0' + sLineBreak + // &#xe8de;
    'today=0' + sLineBreak + // &#xe8df;
    'toll=0' + sLineBreak + // &#xe8e0;
    'tonality=0' + sLineBreak + // &#xe427;
    'touch_app=0' + sLineBreak + // &#xe913;
    'toys=0' + sLineBreak + // &#xe332;
    'track_changes=0' + sLineBreak + // &#xe8e1;
    'traffic=0' + sLineBreak + // &#xe565;
    'train=0' + sLineBreak + // &#xe570;
    'tram=0' + sLineBreak + // &#xe571;
    'transfer_within_a_station=0' + sLineBreak + // &#xe572;
    'transform=0' + sLineBreak + // &#xe428;
    'translate=0' + sLineBreak + // &#xe8e2;
    'trending_down=0' + sLineBreak + // &#xe8e3;
    'trending_flat=0' + sLineBreak + // &#xe8e4;
    'trending_up=0' + sLineBreak + // &#xe8e5;
    'tune=0' + sLineBreak + // &#xe429;
    'turned_in=0' + sLineBreak + // &#xe8e6;
    'turned_in_not=0' + sLineBreak + // &#xe8e7;
    'tv=0' + sLineBreak + // &#xe333;
    'unarchive=0' + sLineBreak + // &#xe169;
    'undo=0' + sLineBreak + // &#xe166;
    'unfold_less=0' + sLineBreak + //
    'unfold_more=0' + sLineBreak + //
    'update=0' + sLineBreak + // &#xe923;
    'usb=0' + sLineBreak + // &#xe1e0;
    'verified_user=0' + sLineBreak + // &#xe8e8;
    'vertical_align_bottom=0' + sLineBreak + // &#xe258;
    'vertical_align_center=0' + sLineBreak + // &#xe259;
    'vertical_align_top=0' + sLineBreak + // &#xe25a;
    'vibration=0' + sLineBreak + // &#xe62d;
    'video_call=0' + sLineBreak + // &#xe070;
    'video_label=0' + sLineBreak + // &#xe071;
    'video_library=0' + sLineBreak + // &#xe04a;
    'videocam=0' + sLineBreak + // &#xe04b;
    'videocam_off=0' + sLineBreak + // &#xe04c;
    'videogame_asset=0' + sLineBreak + // &#xe338;
    'view_agenda=0' + sLineBreak + // &#xe8e9;
    'view_array=0' + sLineBreak + // &#xe8ea;
    'view_carousel=0' + sLineBreak; // &#xe8eb;
  GOOGLE_ICONS10 =
    'view_column=0' + sLineBreak + // &#xe8ec;
    'view_comfy=0' + sLineBreak + // &#xe42a;
    'view_compact=0' + sLineBreak + // &#xe42b;
    'view_day=0' + sLineBreak + // &#xe8ed;
    'view_headline=0' + sLineBreak + // &#xe8ee;
    'view_list=0' + sLineBreak + // &#xe8ef;
    'view_module=0' + sLineBreak + // &#xe8f0;
    'view_quilt=0' + sLineBreak + // &#xe8f1;
    'view_stream=0' + sLineBreak + // &#xe8f2;
    'view_week=0' + sLineBreak + // &#xe8f3;
    'vignette=0' + sLineBreak + // &#xe435;
    'visibility=0' + sLineBreak + // &#xe8f4;
    'visibility_off=0' + sLineBreak + // &#xe8f5;
    'voice_chat=0' + sLineBreak + // &#xe62e;
    'voicemail=0' + sLineBreak + // &#xe0d9;
    'volume_down=0' + sLineBreak + // &#xe04d;
    'volume_mute=0' + sLineBreak + // &#xe04e;
    'volume_off=0' + sLineBreak + // &#xe04f;
    'volume_up=0' + sLineBreak + // &#xe050;
    'vpn_key=0' + sLineBreak + // &#xe0da;
    'vpn_lock=0' + sLineBreak + // &#xe62f;
    'wallpaper=0' + sLineBreak + // &#xe1bc;
    'warning=0' + sLineBreak + // &#xe002;
    'watch=0' + sLineBreak + // &#xe334;
    'wb_auto=0' + sLineBreak + // &#xe42c;
    'wb_cloudy=0' + sLineBreak + // &#xe42d;
    'wb_incandescent=0' + sLineBreak + // &#xe42e;
    'wb_iridescent=0' + sLineBreak + // &#xe436;
    'wb_sunny=0' + sLineBreak + // &#xe430;
    'wc=0' + sLineBreak + // &#xe63d;
    'web=0' + sLineBreak + // &#xe051;
    'web_asset=0' + sLineBreak + // &#xe069;
    'weekend=0' + sLineBreak + // &#xe16b;
    'widgets=0' + sLineBreak + // &#xe1bd;
    'wifi=0' + sLineBreak + // &#xe63e;
    'wifi_lock=0' + sLineBreak + // &#xe1e1;
    'wifi_tethering=0' + sLineBreak + // &#xe1e2;
    'whatshot=0' + sLineBreak + // &#xe80e;
    'work=0' + sLineBreak + // &#xe8f9;
    'wrap_text=0' + sLineBreak + // &#xe25b;
    'youtube_searched_for=0' + sLineBreak + // &#xe8fa;
    'zoom_in=0' + sLineBreak + // &#xe8ff;
    'zoom_out=0' + sLineBreak + // &#xe900;
    'zoom_out_map=0'; // &#xe56b;

implementation

end.
